# macOS junk
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# Build output - regenerated by build.sh / make_dmg.sh
capybara.app/
*.dmg

# Dev-only workaround, not part of the shipped repo
launch.command

# Xcode / SwiftPM in case anyone opens this in Xcode later
.build/
.swiftpm/
*.xcodeproj/
*.xcworkspace/
xcuserdata/
Package.resolved

# Diagnostic log written by the dev-build to /tmp; not in-tree but harmless to ignore
capybara.log
